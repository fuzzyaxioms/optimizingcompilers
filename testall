#!/bin/bash

# assume all the files have already been generated by gentest

# given a path to the test directory
cd test

# diff test the files
for t in *.out
do
	# strip away the extension
	
	echo testing $t
	
	# do the test
	diff --brief $t $t.orig
done
